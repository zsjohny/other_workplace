spring:
  application:
    name: spring_hystrix_turbine_client
  zipkin:
    base-url: http://localhost:8080

server:
  port: 8087


eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true
  client:
    service-url:
#        defaultZone: http://${eureka.instance.hostname}:8068/eureka
        defaultZone: http://localhost:8068/eureka









