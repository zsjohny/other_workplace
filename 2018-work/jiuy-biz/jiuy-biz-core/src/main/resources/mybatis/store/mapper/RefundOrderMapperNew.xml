<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.store.dao.mapper.RefundMapperNew">


	<select id="selectRefundOrder" resultType="com.jiuyuan.entity.newentity.RefundOrder">
        select * from `store_refund_order` where `order_no`=#{orderNo} and `sku_id`=#{skuId}
    </select>

</mapper>
