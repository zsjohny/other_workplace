<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jiuyuan.dao.mapper.supplier.SupplierOrderNewMapper">

	<select id="select" parameterType="Long" resultType="Double">
		select `total_refund_cost` from `store_order` where `OrderNo`=#{orderNo}
	</select>
    
    

</mapper>
