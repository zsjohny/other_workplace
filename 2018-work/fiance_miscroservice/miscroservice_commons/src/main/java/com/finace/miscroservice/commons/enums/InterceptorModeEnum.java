package com.finace.miscroservice.commons.enums;

/**
 * 拦截方式的枚举
 */
public enum InterceptorModeEnum {
    /**
     * header(头部) body_param(body参数) path_param(路径参数)
     */
    HEADER, BODY_PARAM, PATH_PARAM
}
